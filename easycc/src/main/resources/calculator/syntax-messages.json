{
  "LL": {
    "default": "Error found",
    "messages": [
      {
        "non-terminal": "M",
        "terminal": "integer",
        "message": "Expecting an operator instead of ${lexical.value}"
      }
    ]
  },
  "LR": {
    "default": "Error found",
    "messages": [
        {
          "error-key": "OP",
          "terminal": "plus",
          "message": "Expecting an operator instead of an integer"
        }
      ]
  }
}